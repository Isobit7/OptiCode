# FastAPI backend URL (deployed on Render/Fly.io — see architecture doc)
VITE_API_BASE_URL=http://localhost:8000

# Supabase public config (safe to expose — anon key only, NOT the service key)
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
